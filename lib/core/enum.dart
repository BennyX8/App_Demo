

enum AppButtonType { primary, secondary, outline, text }

enum Positions { before, after, leading, extended }

enum AsyncState { loading, idle, error }

enum AppState { success, loading, failed, idle }
