import 'package:flutter/material.dart';

class ReplyCommentView extends StatelessWidget {
  const ReplyCommentView({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(child: Row(children: [],));
  }
}